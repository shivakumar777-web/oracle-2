[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: tests requiring optional ML deps (rdkit, neurokit2, pydicom)
