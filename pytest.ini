[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: optional ML deps (rdkit, neurokit2, pydicom) or live HTTP (ClinicalTrials.gov, herb-drug APIs)
